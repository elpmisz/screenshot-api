{
  "name": "screenshot-api",
  "version": "1.0.0",
  "description": "High-performance screenshot API with auto-sleep optimization",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "build": "npx puppeteer browsers install chrome && echo 'Chromium installed successfully'",
    "postinstall": "npm run build",
    "install-browser": "npx puppeteer browsers install chrome",
    "check-browser": "node -e \"const puppeteer = require('puppeteer'); console.log('Puppeteer version:', require('puppeteer/package.json').version);\"",
    "health": "curl http://localhost:3000/health"
  },
  "keywords": [
    "screenshot",
    "puppeteer",
    "api",
    "web-scraping"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "puppeteer": "^24.20.0"
  }
}
